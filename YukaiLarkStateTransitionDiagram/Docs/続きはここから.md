# 続きはここから

## 完了したこと

- MonoGame の初版状態遷移図ツールを実装済み。
- トップ README をディレクトリーツリー案内板に整理済み。
- Docs フォルダーに使い方説明書を追加済み。

## 確認済み

- `dotnet build .\SkylarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功済み。

## 次にやるなら

- 状態ラベルの編集機能を追加する。
- 保存先をユーザーが選べるようにする。
- 図を画像として書き出す機能を追加する。