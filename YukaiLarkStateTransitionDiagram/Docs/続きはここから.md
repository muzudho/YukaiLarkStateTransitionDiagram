# 続きはここから

## 完了したこと

- ［開始ノード］/［終了ノード］の用語を、第一段階として［開始マーク］/［終了マーク］へ統一した。
- コード識別子を `StartMarker` / `EndMarker` 系へリネームした。
- `NodeKind` は `Normal = 0`, `StartMarker = 1`, `EndMarker = 2` とし、保存 JSON の数値互換を維持した。
- 使い方説明書、UI ステータス、アシスト文言、描画コメント、インスペクター表示を更新した。
- 詳細計画と進捗は [開始終了マーク用語統一 実装計画 2026-06-22](開発/開始終了マーク用語統一_実装計画_2026-06-22.md) に記録した。
- 開発日誌を [開発日誌 2026-06-22](開発/開発日誌_2026-06-22.md) として残した。

- ［S］キーで、［開始マーク］が無いときはマウス位置に追加し、既にあるときは開始マークを画面中央へ表示移動する操作を追加した。
- ［T］キーによるノード種別切り替えを廃止した。通常状態は `N`、開始マークは `S`、終了マークは `E` で扱う。
- 下部ヘルプバーと使い方説明書から `T` の種類変更説明を削除し、開始マーク・終了マークの操作説明を追加した。
- ユカイラークのアシスト判定を通常ノード数ベースにし、［開始マーク］と［終了マーク］しかないときは先に通常ノード作成を促すようにした。
- ユカイラークの遷移作成対象を通常ノードに限定し、手動操作でも［開始マーク］から［終了マーク］への直接遷移は作らないようにした。
- ［開始マーク］から最初の通常ノードへ入る遷移は、イベント編集・イベント未設定アシスト・イベントゴーストの対象外にした。
- ユカイラークが開始マークから通常ノードへの遷移を作成した直後、イベントを付けられないことを完了吹き出しで説明するようにした。
- ユカイラークが通常ノードを作る位置を開始マークから離し、ベジェ曲線のハンドルを見やすくした。

- ユカイラークが、開始マークから最初の通常ノードへの遷移を作ったあと、2個目の通常ノードを作るか尋ねるようにした。
- 2個目の通常ノード作成提案は、約6秒操作されなければスキップして次のアシストへ進むようにした。
- 2個目の通常ノード作成後は、通常ノード同士の遷移作成を提案し、その遷移はイベント追加アシストの対象にした。
- `YukaiLarkAssistant.cs` が 0 バイトになっていてビルド不能だったため、直近コミットの内容をベースに復旧してから拡張した。
- 詳細計画と進捗は [通常ノード2個目アシスト 実装計画 2026-06-23](開発/通常ノード2個目アシスト_実装計画_2026-06-23.md) に記録した。

- ユカイラークが、［開始マーク］から一番遠い［通常ノード］から［終了マーク］への遷移を作るようアシストするようにした。
- 終了マーク向け遷移のゴースト表示と実際の作成処理で、同じ接続元・接続先を使うようにした。
- 詳細計画と進捗は [終了マーク遷移アシスト 実装計画 2026-06-23](開発/終了マーク遷移アシスト_実装計画_2026-06-23.md) に記録した。

- ラベル編集中の末尾アンダースコア表示をやめ、挿入位置に点滅キャレットを描くようにした。
- 遷移イベント名と状態ラベルの編集で、Left/Right/Home/End、Backspace/Delete がキャレット位置に対応した。
- 開発日誌を [開発日誌 2026-06-23](開発/開発日誌_2026-06-23.md) として残した。

- `Ctrl + P` 直後に、図全体を包むPNG出力範囲枠を自動表示するようにした。
- PNG出力範囲枠は、枠内ドラッグで移動、辺・角ドラッグでサイズ調整でき、空の図では中央に既定サイズで表示される。
- `Enter` でPNG保存に成功したあと、白いフラッシュと写真風プレビューを短く表示するようにした。
- 詳細計画と進捗は [PNG出力自動枠撮影演出 実装計画 2026-06-24](開発/PNG出力自動枠撮影演出_実装計画_2026-06-24.md) に記録した。

- Visual Studio の Codex Chat がパス文字列まわりで落ちる件を調べ、Windows イベントログ上で `CodexVsix.UI.MarkdownRenderer.CreateInlineCode` と `Path.IsPathRooted` の例外を確認した。
- 回避策を [Codex Chat パス文字列フリーズ回避](運用/CodexChatパス文字列フリーズ回避.md) にまとめ、repo ローカル skill とインストール用スクリプトを追加した。

- ユカイラークの吹き出しは台詞だけを入れる小さな表示にし、説明文は半透明の黒帯カットインへ分離した。
- 通常アシストの説明は1行カットイン、作図完了アシストの詳細とヒントは2行カットインで表示するようにした。

- ユカイラークの説明カットインを、黒帯固定ではなくテーマに合わせた色で出せるようにした。
- ［終了マーク］からは［遷移］エッジを伸ばせないようにし、終了マークを到着専用として扱う流れにした。
- ［開始マーク］へは［遷移］エッジを伸ばせないようにし、開始マークを出発専用として扱う流れにした。
- 遷移作成中、接続できない［開始マーク］や［終了マーク］をテーマに合わせて不活性表示するようにした。
- 開始エッジの装飾案は、丸囲み矢印、三角の連続、ひまわりシルエットなどを試したが、今回は強調しすぎないよう元に戻した。
- 選択中の遷移エッジに、半透明の太いハローと曲線上の小さなマーカーを重ねて、細く長い曲線でも選択状態を追いやすくした。開始マーク用の二重線にも同じ選択ハローを敷くようにした。
- 遷移エッジのマウスオーバー表示は、選択色の太線ではなく、遷移本来の色で短周期に揺れる重ね線へ変更した。選択中は固定ハローとマーカー、ホバー中は微振動で見分ける。
- 通常ノード、開始マーク、終了マークのマウスオーバー表示は、外側へ別リングを追加せず、既存の外周線を同じ色のウニョウニョ円へ置き換える形にした。

- ミニマップを追加した。`MiniMap/MiniMapLayout.cs` が座標変換、`MiniMap/MiniMapRenderer.cs` が簡易ノードと現在表示範囲の描画を担当する。表示位置は右下の独立小窓で、カットイン帯と重なるとグレーアウトする。
- ミニマップ上をクリックまたはドラッグすると、その地点が画面中央に来るように表示位置を移動できる。
- インスペクターは右側のまま、ミニマップの直上に間隔を空けて置くようにした。
- ユカイラークはヘッダー下に余白を取った右上寄り配置にした。
- 詳細計画と進捗は [MiniMap 実装計画 2026-06-27](開発/MiniMap実装計画_2026-06-27.md) に記録した。
- 遷移作成中、接続元から同じ向きの遷移が既にある接続先ノードも不活性表示するようにした。通常ノード同士で既につながっている相手にも、重複遷移を作れないことが見た目で分かる。
- 通常ノード色を固定 Palette から BoardTheme.NormalNodePalette へ移し、全テーマ 6 色で描画・Cキー巡回・アシスト作成・ミニマップを揃えた。開始マークと終了マークもテーマ由来の別色で表示するようにした。
- YukaiLark テーマの通常ノード 6 色は変更前の固定パレットへ戻し、ノードラベル色は塗り色の明度から白系/濃色系を選ぶようにした。
- 通常ノード選択中の `C` は、状態色パレットを開く操作に変更した。パレット表示中は `C` / `Esc` / 右クリックで閉じ、`1`-`6` またはクリックで任意の色番号を即時割り当てできる。パレット外クリックは閉じず、背面のノード選択へ通す。
- 状態色パレットの各色箱の下に `>` ボタンを追加した。1番下の `>` は 1 番と 2 番、2番下の `>` は 2 番と 3 番を入れ替える。入れ替えはテーマ別 AppConfig に保存され、ノードの `ColorIndex` はそのまま。
- 状態色パレットでは `Shift+クリック` の2回操作で、現在テーマの通常ノード色 6 枠を入れ替えられる。入れ替え結果は `AppConfig.NormalNodePaletteOverrides` にテーマ別で保存し、保存データ側の `ColorIndex` はそのまま使う。
- 通常ノードのラベル文字色は、背景色とのコントラスト比で白系・濃色系の読みやすい方を選ぶようにした。テーマやパレット入れ替え後の読みにくさを減らすための調整。
- ユカイラークが、自動生成ラベル `状態{id}` のままの通常ノードを見つけたときに、［ノードのラベルを編集する？］アシストを出し、実行すると既存の状態ラベル編集へ入るようにした。
- ラベル編集アシスト中は対象の通常ノードへゴーストリングを重ね、カットインに、画面下部ヘルプと同じキーキャップ形式で［Enter］ラベルを入力、［Esc］しないを出す。キーキャップ行はクリック可能で、ホバー時に枠を出す。［Enter］は実行、［Esc］と Esc キーはそのノードの提案を作業中だけスキップする。

- 2026-07-04 時点で、サブステートから親の図へ戻る操作を追加した。`Ctrl+Shift+Enter` または `Alt+Up` で、現在の `DiagramInstance` を参照している親ノードを探し、その親ノードがあるタブへ切り替える。`Alt+Down` は `Ctrl+Enter` と同じく、選択中の通常ノードのサブステートへ入る操作にした。
- 下部ショートカットヘルプは、親サブステートがある図では［外へ戻る］を表示し、通常ノード選択時は `Ctrl+Enter` と `Alt+Down` の［中へ入る］を表示するようにした。

## 確認済み

- `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-23 のアシスト修正後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-23 の開始マーク遷移イベント制限後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-23 のアシスト通常ノード配置調整後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-23 の通常ノード2個目アシスト追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-23 の終了マーク遷移アシスト追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-23 のラベル編集キャレット追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- `Keys.T` / `ToggleNodeKind` / `種類変更` / `Tで通常` は、実装・下部ヘルプバー・使い方説明書から消えている。
- 旧識別子 `StartNode` / `EndNode` / `NodeKind.Start` / `NodeKind.End` は C# と使い方説明書から消えている。
- 開発日誌と引き継ぎメモの両方で、今回の用語統一の流れを追えるようにした。
- `Application` ログの `1026` / `1000` / `1001` から、Codex Chat のパス文字列表示時に `devenv.exe` が落ちる再現痕跡を確認した。

- 2026-06-25 のユカイラーク説明カットイン追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-27 の開始マーク・終了マーク接続ルール整理後、画面上で［終了マーク］から遷移を伸ばせないことと、［開始マーク］へ遷移を伸ばせないことを確認した。
- 2026-06-27 の選択中エッジエフェクト追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-27 のエッジホバー表示差別化後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-27 のノード／マークホバー表示を外周線置き換え方式へ修正した後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。

- 2026-06-27 のインスペクター内ミニマップ追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。

- 2026-06-27 のミニマップ右下移動とカットイン重なり時グレーアウト追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-27 のインスペクターとミニマップの重なり回避、ユカイラーク右上配置調整後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-27 のユカイラークをヘッダー下へ離す配置調整後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-27 の重複遷移先ノードの不活性表示追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-27 の状態色パレット追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx` は警告 0、エラー 0 で成功。
- 2026-06-28 の状態色パレット開閉トグル化後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx -v:minimal` は警告 0、エラー 0 で成功。
- 2026-06-28 の状態色パレット隣接入れ替えボタン追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx -v:minimal` は警告 0、エラー 0 で成功。
- 2026-06-28 の通常ノードラベル文字色コントラスト改善後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx -v:minimal` は警告 0、エラー 0 で成功。

- 2026-06-28 の通常ノードラベル編集アシスト追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx -v:minimal` は警告 0、エラー 0 で成功。
- 2026-06-28 のラベル編集アシスト操作説明のキーキャップ表示化後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx -v:minimal` は警告 0、エラー 0 で成功。

- Version 2.0.0 に向けたサブステート段階導入案を [サブステート段階導入 実装計画 2026-07-02](開発/substate_incremental_plan_2026-07-02.md) に整理した。まずは今の図を 1 つのダイアグラムインスタンスとして扱えるようにし、その後タブ、通常ノードとの関連付け、グローバル遷移へ進める。
- 2026-07-03 時点で、複数タブ保存と日本語タブ名の読み書き確認後のサブステート仕様メモを追記した。サブステートは［通常ノード］だけが最大 1 個持ち、`DiagramNode.SubstateDiagramId` で既存の `DiagramInstance` へ関連付ける。サブステートを持つ通常ノードは、終了マークと混同しない通常ノード専用の二重枠で表示する方針。

- 2026-07-04 のサブステート親戻り操作追加後も `dotnet build .\YukaiLarkStateTransitionDiagram.slnx -v:minimal` は警告 0、エラー 0 で成功。

## 次にやるなら

- ミニマップに遷移線の簡易表示を足すか、表示が混みすぎる場合はノードだけのままにするかを実画面で確認する。
- ［開始マーク］から出る遷移を 1 本に制限する。開始マークから通常ノードへの遷移はイベントなしに制限済みで、［開始マーク］から［終了マーク］への直接遷移は禁止済み。
- ［開始マーク］を新規ダイアグラムに最初から 1 個だけ固定配置する方針を再確認する。
- ［開始マーク］の追加・削除ポリシーを、現在の `S` キー操作と合わせて整理する。
- 出発専用・到着専用のルールを、使い方説明書とユカイラークのアシスト文言にも必要なら反映する。
- 画面上の残りの［開始ノード］/［終了ノード］表記がないか、軽く見直す。
- Codex Chat 側の不具合が直るまでは、Windows パスをそのままインラインコードで返さない運用を続ける。

